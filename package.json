{
  "name": "quizz-platform",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd server && npm install && cd ../client && npm install && npm run build",
    "start": "node server/src/server.js",
    "server": "node server/src/server.js",
    "client": "cd client && npm run dev",
    "test": "node test_quiz_flow.mjs"
  }
}
